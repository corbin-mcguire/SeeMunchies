<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StagingActivity">

    <ImageView
        android:id="@+id/imagePreview"
        android:layout_width="309dp"
        android:layout_height="319dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="4dp"
        app:srcCompat="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/submitConfirmTextView"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="355dp"
        android:text="Would you like to submit this image?"
        android:textAlignment="center" />

    <Button
        android:id="@+id/retryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/submitConfirmTextView"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="0dp"
        android:layout_marginBottom="61dp"
        android:text="retry" />

    <Button
        android:id="@+id/submitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/retryButton"
        android:layout_alignEnd="@+id/submitConfirmTextView"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="0dp"
        android:text="submit" />

    <Button
        android:id="@+id/galleryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="-347dp"
        android:layout_toEndOf="@+id/imagePreview"
        android:text="See The Gallery!" />

</RelativeLayout>